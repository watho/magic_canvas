name: magic_canvas
description: Flutter package supports drawing canvas shapes such as squares, circles, lines,...
version: 0.0.3
homepage: https://github.com/mag1c-dev/magic_canvas

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
